# Django Settings
SECRET_KEY=your-secret-key-here-generate-at-https://djecrety.ir/
DEBUG=False
DJANGO_ALLOWED_HOSTS=*

# Render-specific (auto-set by Render, but shown here for reference)
# RENDER_EXTERNAL_URL is automatically set by Render for CSRF
